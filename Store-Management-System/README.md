# Store Management System

A modular, Firebase-ready store management system with dashboard, inventory, sales, payments, customers, suppliers, reports, and authentication.

## Run locally
- Open login.html in a browser, or
- Run `python -m http.server 8000` from the project folder.

## Firebase setup
- Replace the placeholder values in js/firebase/firebase-config.js with your real Firebase credentials.
